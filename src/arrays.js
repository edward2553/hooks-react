// arreglos en js

// const arreglo = new Array(100);
const arreglo = [1,2,3,4];
// arreglo.push(1); // modifica el arreglo principal

let arreglo2 = [...arreglo, 5];

// arreglo2.map(functions)

// arreglo2.push(5);

console.log(arreglo);
console.log(arreglo2);